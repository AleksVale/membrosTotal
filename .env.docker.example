# Environment variables for docker-compose
# This file should be created in production with the correct values

# Frontend Environment Variables
NEXT_PUBLIC_API_URL=http://api.nohau.agency/api

# You can also use the internal Docker network if both containers are in the same network
# NEXT_PUBLIC_API_URL=http://membros:3000/api
